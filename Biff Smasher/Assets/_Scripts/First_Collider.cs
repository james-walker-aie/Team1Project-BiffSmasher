using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class First_Collider : MonoBehaviour
{
    //This script os to trigger a text box when the player goes backward in the game.

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
